<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph"
    app:startDestination="@+id/listing_fragment">

    <fragment
        android:id="@+id/listing_fragment"
        android:name="br.com.olx.listing.ListingFragment"
        android:label="Listing">

        <action
            android:id="@+id/action_listing_to_adview"
            app:destination="@id/adview_fragment" />

    </fragment>

    <fragment
        android:id="@+id/adview_fragment"
        android:name="br.com.olx.adview.AdviewFragment"
        android:label="Adview" />
</navigation>